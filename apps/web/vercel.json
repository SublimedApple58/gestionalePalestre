{
  "buildCommand": "export DIRECT_URL=${DIRECT_URL:-$DATABASE_URL} && pnpm --filter @gestionale/db db:deploy && pnpm --filter @gestionale/db db:generate && next build",
  "crons": [
    {
      "path": "/api/internal/document-jobs/run",
      "schedule": "*/5 * * * *"
    }
  ]
}
